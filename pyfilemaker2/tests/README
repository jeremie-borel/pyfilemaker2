
# running unittest:
python -m unittest discover

# upload on pypi (update setup.py)
pip install twine
python setup.py clean --all
python setup.py sdist
python -m twine upload --repository pypi dist/*
